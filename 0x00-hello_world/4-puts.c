#include <stdio.h>
/* main opens the programme
 * Return : always 0
 * include denotes library
 *
 * puts for printing str and strtung the next in a new line
 * char for denoting character
 */
int main(void)
{
	char the_str[100]= "\"Programming is like building a multilingual puzzle";

	puts(the_str);
	return (0);
}
