#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strlen - function for string counting
 * @s: parameter
 * Return: returning integer
 */

int _strlen(char *s)
{
	int t;

	t = strlen(s);
	return (t);
}
