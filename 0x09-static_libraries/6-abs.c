#include "main.h"
#include <stdlib.h>

/**
 * _abs - function gottenfrom absolute value
 * @int: integer input
 * Return: returns the accepted loop condition
 *
 */

int _abs(int);

int _abs(int k)
{
	int j;

	j = abs(k);

	return (j);
}
